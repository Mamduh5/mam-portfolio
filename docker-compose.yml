services:
  frontend:
    build: .
    container_name: mamportfolio
    ports:
      - "5173:5173"
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:4000
    networks:
      - mam-portfolio

networks:
  mam-portfolio: